appId: com.bpaxson62.cbquestlog
productName: CB Quest Log
directories:
  buildResources: build
files:
  - '!**/.vscode/*'
  - '!src/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintignore,.eslintrc.cjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'
asarUnpack:
  - resources/**

# Bundled quest data (data/unlocks/*, data/seasons.json), read at startup
# so every install ships pre-populated — see src/main/content.ts.
extraResources:
  - from: data
    to: data

# --- Windows ---
win:
  executableName: cb-quest-log
  target:
    - target: nsis
      arch: [x64, arm64]
nsis:
  artifactName: ${name}-${version}-setup.${ext}
  shortcutName: ${productName}
  uninstallDisplayName: ${productName}
  createDesktopShortcut: always

# --- macOS ---
mac:
  artifactName: ${name}-${version}-${arch}.${ext}
  target:
    - target: dmg
      arch: [x64, arm64]
  category: public.app-category.strategy-games
  entitlementsInherit: build/entitlements.mac.plist
  notarize: false

# --- Linux ---
linux:
  artifactName: ${name}-${version}-${arch}.${ext}
  target:
    - target: AppImage
      arch: [x64, arm64]
    - target: deb
      arch: [x64, arm64]
  maintainer: Brian <bpaxson62@users.noreply.github.com>
  category: Game

npmRebuild: false

publish:
  provider: github
  owner: bpaxson62
  repo: conqbladequester
  releaseType: draft
